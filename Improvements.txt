1 - When the user logout from the app, it must clean up the database;
2 - Feature to delete a task (ask for sure about this action);
3 - Show the numbers of tasks according to the filter of show/hide finished tasks inside each card (today, tomorrow, week). It
means to show only the number of the tasks that are visible in the list;
4 - Crashlytics.